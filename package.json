{
  "name": "bugtrackerproject",
  "version": "1.0.0",
  "description": "Bug Tracker",
  "main": "app.js",
  "scripts": {
    "test": "docker run --name bugDB -p 30000:27017 -d mongo:latest && mocha test --exit || docker rm -f bugDB && docker run --name bugDB -p 30000:27017 -d mongo:latest && mocha test --exit",
    "start": "docker run --name bugDB -p 30000:27017 -d mongo:latest && nodemon app.js || docker rm -f bugDB && docker run --name bugDB -p 30000:27017 -d mongo:latest && nodemon app.js"
  },
  "keywords": [
    "Bug"
  ],
  "author": "Node Kings (Alex)",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "chroma-log": "^3.1.0",
    "dragula": "^3.7.2",
    "ejs": "^3.1.3",
    "express": "^4.17.1",
    "mongoose": "^5.10.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^8.1.1"
  }
}
